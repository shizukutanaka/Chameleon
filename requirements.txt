# Core dependencies
PyYAML>=6.0

# Audio processing (optional but recommended)
soundfile>=0.12.0
sounddevice>=0.4.0
librosa>=0.10.0
numpy>=1.21.0

# Format conversion (optional)  
pydub>=0.25.1

# System monitoring (optional)
psutil>=5.9.0

# Progress indicators (optional)
tqdm>=4.64.0

# GUI support (optional)
matplotlib>=3.5.0

# Note: External dependencies for advanced features:
# - FFmpeg: Required for comprehensive format conversion
# - SoX: Alternative audio processing backend
# 
# Installation guide:
# Basic: pip install -r requirements.txt
# Full:  pip install -r requirements.txt && apt-get install ffmpeg sox