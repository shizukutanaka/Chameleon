# Chameleon Audio System v3.0 - Minimal Dependencies
# Lightweight WAV-focused audio processing

# Basic dependencies (optional - system works without them)
# colorama>=0.4.4        # For colored terminal output
# PyYAML>=6.0           # For configuration files

# Note: Core system uses only Python standard library
# For extended format support, install: ffmpeg
# For professional audio features, consult the documentation before adding third-party packages