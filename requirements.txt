# Optimized Chameleon Audio System - Lightweight Dependencies
# Core functionality uses built-in Python modules only

# Optional: System monitoring (lightweight)
psutil>=5.8.0

# Optional: Configuration support
pyyaml>=6.0

# Development tools (optional - install separately if needed)
# pytest>=7.0.0
# flake8>=4.0.0
# black>=22.0.0

# Security scanning (optional)
# bandit>=1.7.0

# NOTE: The core audio engine uses only Python standard library
# This provides maximum compatibility and minimal dependencies